{
  "extends": "../../tsconfig.react.json",
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // Monorepo alias for shared packages (point to each package's src so TS can see types)
      "@k2-saas/*": ["../../packages/*/src"]
    }
  }
}
