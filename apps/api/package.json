{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate:sqlite",
    "db:push": "wrangler d1 execute k2-sass-db --local --file ./migrations/0000_sparkling_jamie_braddock.sql",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@k2-saas/shared-types": "workspace:*",
    "@libsql/client": "^0.15.15",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.5",
    "wrangler": "^4.4.0"
  }
}
